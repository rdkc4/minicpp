int main(){
    int x;
    int y;
    int z;

    x = xyz(x, y, z);

    if(x>y)
        if(z>f)
            return x;
        else
            return x;
}

int xyz(int x, int y, int z){
    return x+(y-z);
}